<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auggie.student_server.mapper.QuizQuestionMapper">
    <resultMap id="QuizQuestionResultMap" type="QuizQuestion">
        <id property="questionId" column="question_id"/>
        <result property="quizId" column="quiz_id"/>
        <result property="questionType" column="question_type"/>
        <result property="questionContent" column="question_content"/>
        <result property="questionScore" column="question_score"/>
        <result property="options" column="options"/>
        <result property="correctAnswer" column="correct_answer"/>
        <result property="questionOrder" column="question_order"/>
        <result property="isAIGenerated" column="is_ai_generated"/>
        <result property="aiConfidence" column="ai_confidence"/>
        <result property="sourceResource" column="source_resource"/>
    </resultMap>

    <select id="findAll" resultMap="QuizQuestionResultMap">
        SELECT * FROM studentms.quiz_question;
    </select>

    <select id="findById" resultMap="QuizQuestionResultMap">
        SELECT * FROM studentms.quiz_question WHERE question_id = #{questionId};
    </select>

    <select id="findByQuizId" resultMap="QuizQuestionResultMap">
        SELECT * FROM studentms.quiz_question WHERE quiz_id = #{quizId} ORDER BY question_order ASC;
    </select>

    <update id="updateById">
        UPDATE studentms.quiz_question
        <set>
            <if test="question.quizId != null">
                quiz_id = #{question.quizId},
            </if>
            <if test="question.questionType != null">
                question_type = #{question.questionType},
            </if>
            <if test="question.questionContent != null">
                question_content = #{question.questionContent},
            </if>
            <if test="question.questionScore != null">
                question_score = #{question.questionScore},
            </if>
            <if test="question.options != null">
                options = #{question.options},
            </if>
            <if test="question.correctAnswer != null">
                correct_answer = #{question.correctAnswer},
            </if>
            <if test="question.questionOrder != null">
                question_order = #{question.questionOrder},
            </if>
            <if test="question.isAIGenerated != null">
                is_ai_generated = #{question.isAIGenerated},
            </if>
            <if test="question.aiConfidence != null">
                ai_confidence = #{question.aiConfidence},
            </if>
            <if test="question.sourceResource != null">
                source_resource = #{question.sourceResource},
            </if>
            <!-- 确保至少有一个字段被设置，避免SET子句为空 -->
            question_id = #{question.questionId}
        </set>
        WHERE question_id = #{question.questionId};
    </update>

    <insert id="save" useGeneratedKeys="true" keyProperty="question.questionId">
        INSERT INTO studentms.quiz_question (quiz_id, question_type, question_content, question_score, options, correct_answer, question_order, is_ai_generated, ai_confidence, source_resource)
        VALUES (#{question.quizId}, #{question.questionType}, #{question.questionContent}, #{question.questionScore}, #{question.options}, #{question.correctAnswer}, #{question.questionOrder}, #{question.isAIGenerated}, #{question.aiConfidence}, #{question.sourceResource})
    </insert>

    <insert id="saveBatch">
        INSERT INTO studentms.quiz_question (quiz_id, question_type, question_content, question_score, options, correct_answer, question_order, is_ai_generated, ai_confidence, source_resource)
        VALUES
        <foreach collection="questions" item="q" separator=",">
            (#{q.quizId}, #{q.questionType}, #{q.questionContent}, #{q.questionScore}, #{q.options}, #{q.correctAnswer}, #{q.questionOrder}, #{q.isAIGenerated}, #{q.aiConfidence}, #{q.sourceResource})
        </foreach>
    </insert>

    <delete id="deleteById">
        DELETE FROM studentms.quiz_question WHERE question_id = #{questionId}
    </delete>

    <delete id="deleteByQuizId">
        DELETE FROM studentms.quiz_question WHERE quiz_id = #{quizId}
    </delete>
</mapper>
